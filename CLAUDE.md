@AGENTS.md

## Claude-specific

- When a student asks for an app, go straight through the workflow in AGENTS.md. Finish with a **headed** run (`npm run show <slug>`) so they watch you test it.
- Before calling it done, read at least one screenshot from `test-results/<slug>/` with the Read tool.
